import {Company} from "../company-list/company-list.component";

export interface AddReviewInterface {
}

/**
 * Method to save the review submitted by the user in the database.
 * @param username
 * @param rating
 * @param title
 * @param reviewText
 * @param Company
 * @constructor
 */
function SaveReview(username: String, rating: number, title: String, reviewText: String, Company: String){

}

function GetCompanyList() {
  return [];
}
